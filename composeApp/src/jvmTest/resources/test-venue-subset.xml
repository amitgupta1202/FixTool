<!--
    A VENUE SUBSET, which is what a real venue dictionary is: it lists only the fields that venue
    speaks. LastPx(31) is declared; AvgPx(6) and OrdStatus(39) deliberately are not, though both are
    standard FIX 4.4. Used by ComprehensionFloorTest to hold the line that a subset states WHICH
    fields exist without redefining what a standard field means.
  -->
<fix major="4" minor="4">
  <header><field name="BeginString" required="Y"/><field name="MsgType" required="Y"/></header>
  <trailer><field name="CheckSum" required="Y"/></trailer>
  <messages>
    <message name="ExecutionReport" msgtype="8" msgcat="app">
      <field name="LastPx" required="N"/>
    </message>
  </messages>
  <fields>
    <field number="31" name="LastPx" type="PRICE"/>
    <field number="8" name="BeginString" type="STRING"/>
    <field number="35" name="MsgType" type="STRING"><value enum="8" description="EXECUTION_REPORT"/></field>
    <field number="10" name="CheckSum" type="STRING"/>
  </fields>
</fix>
