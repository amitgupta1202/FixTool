{
  "name": "fixtool-mcp",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Stdio transport shim in front of FixTool's own MCP endpoint; forwards tools/list and tools/call verbatim.",
  "bin": {
    "fixtool-mcp": "index.mjs"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
