{
  "name": "fixtool-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing FixTool's automation control surface (connect, send, read messages, screenshot).",
  "bin": {
    "fixtool-mcp": "index.mjs"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  }
}
